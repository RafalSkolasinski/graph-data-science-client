# Louvain community detection on a social network (Person -KNOWS-> Person).
# Implicit lifecycle: only projections/algorithms/writebacks - no project/drop steps.
session:
  name: gds-examples
  memory: 2GB
  ttl_minutes: 30

projections:
  - graph: social
    cypher: |
      MATCH (source:Person)-[rel:KNOWS]->(target:Person)
      RETURN gds.graph.project.remote(source, target, {
        sourceNodeLabels: labels(source),
        targetNodeLabels: labels(target),
        relationshipType: type(rel)
      })

algorithms:
  - name: louvain
    graph: social
    mode: mutate
    mutate_property: community

writebacks:
  - graph: social
    node_properties: [community]
