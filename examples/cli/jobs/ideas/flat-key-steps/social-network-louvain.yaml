# Louvain community detection on a social network (Person -KNOWS-> Person).
# Flat-key steps: each step's type is just another key sitting next to `name`.
session:
  name: gds-examples
  memory: 2GB
  ttl_minutes: 30

projections:
  - cypher:
      graph: social
      query: |
        MATCH (source:Person)-[rel:KNOWS]->(target:Person)
        RETURN gds.graph.project.remote(source, target, {
          sourceNodeLabels: labels(source),
          targetNodeLabels: labels(target),
          relationshipType: type(rel)
        })

steps:
  - name: Project the social graph
    project_graph: social

  - name: Run Louvain
    algorithm: louvain
    graph: social
    mode:
      name: mutate
      property: community

  - name: Write community back to the database
    write_back: social
    node_properties: [community]

  - name: Drop the graph
    drop_graph: social
